# Regenerated by the scripts (run 10_squad_strength_blend.py to rebuild)
data/processed/

# Python
__pycache__/
*.pyc
.venv/
.ipynb_checkpoints/

# Kept on purpose (reference data + readable reports are committed):
#   annex_c_third_allocation.csv, FITTED_PRESETS.md, CHAMPION_ODDS_FINAL.md, etc.

# Figures are build artifacts (scripts 14/16/17 -> 18_normalise_slides.py). The finished
# carousel_*.png and the evaluation figure ARE committed (portfolio README embeds them); the raw
# pre-padding scratch is not. Never hand-edit a figure — regenerate it.
reports/figures/_raw/
